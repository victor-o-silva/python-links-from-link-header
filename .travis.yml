language: python
sudo: false
cache:
  directories:
    - $HOME/.cache/pip
python:
    - "2.6"
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"
    - "pypy"
    - "pypy3"
install:
    - pip install coveralls
script:
    - coverage run --source=links_from_header tests.py
after_success:
    - coveralls